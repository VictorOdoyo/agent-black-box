services:
  viewer:
    image: node:20-bookworm-slim
    working_dir: /workspace
    command: sh -c "corepack enable && corepack prepare pnpm@9.15.9 --activate && pnpm install && pnpm dev --host 0.0.0.0"
    ports:
      - "5173:5173"
    volumes:
      - .:/workspace
