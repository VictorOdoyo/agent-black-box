# Policy Packs

Policy packs are JSON files that make local trace checks stricter for a project.

Supported fields:

- `name`
- `max_tool_duration_ms`
- `max_error_events`
- `denied_tools`
- `required_tags`
